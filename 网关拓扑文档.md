# 网关拓扑文档 · qianli 开发内网现状

> 维护者文档（2026-09-20 定稿）。事实口径权威：`dashboard/registry.js`（机器人权能）与本档（网络拓扑）；实时探测=运维台 `127.0.0.1:3100` →「🌐 网络拓扑」。
> **密码一律不入本档与 git**：主网关/4A/裁判路由器管理密码=用户持有；小电脑 SSH=`approval-bot/.env` 的 `NAS_PASSWORD`。
> 排查手册与踩坑细节见 `.agents/skills/qianli-lab-network/SKILL.md`（§三/§四/§九/§十/§十一）。

## 术语约定（防歧义）

- **物理机器人** = 机甲大师场外机器人（连接裁判系统路由器，192.168.3.x）；
- **软件机器人** = 小电脑里的六仓飞书机器人服务（feishu-gateway/hub/ticket/duty/approval/wecom）；
- 两类"机器人"无任何关联，本文档严格区分。

## 一、总览

```
校园网墙口（学生账号会话 · 全体系唯一外网生命线 · 被踢秒级自愈）
   │ WAN
[主网关 SuperQianLi] 192.168.31.1/24 · RD08 · 全 4 口 2.5G · 管理密码=用户持有
   │  WiFi: SuperQianLi_5G（5G）/ SuperQianLi（2.4G）
   ├─ LAN1(2.5G) ── （预留；运维笔记本平时走 WiFi，需要网线直连 2.x/3.x 路由器时临时用拓展坞插这里）
   ├─ LAN2(2.5G) ── [5口千兆交换机]（有线汇聚，大流量本地交换不过主网关）
   │                ├─ 小电脑 DESKTOP-FE1MIGI 192.168.31.57
   │                │    └ 软件机器人×6（pm2）+ bambu-print-server(:3001) + 裁判系统后端（待部署）
   │                ├─ 旧NAS qianli-NAS 192.168.31.153（原 .151，挪线后 DHCP 重分配；建议主网关按 MAC 绑静态）
   │                ├─ [裁判系统路由器] WAN 192.168.31.80（MAC CC:C4:B2:46:16:2F）→ 见 §四
   │                └─ [4A 打印隔离区] WAN（待插线，插上自动 DHCP 拿 31.x）→ 见 §三
   └─ WiFi ── 手机/室友等（主网上网）
```

## 二、三网段总表

| 网段 | 网关 | 用途 | DHCP 池 | 隔离方式 |
|---|---|---|---|---|
| 192.168.31.0/24 | 192.168.31.1（主网关） | 开发内网 + 校园网上行 | 有 | — |
| 192.168.2.0/24 | 192.168.2.1（4A，OpenWrt 23.05.2） | **打印机隔离区** | .100-.249 | 出向全拒（lan⇒REJECT） |
| 192.168.3.0/24 | 192.168.3.1（裁判系统路由器） | **物理机器人 + 裁判端电脑** | 有 | NAT 隔离（外部不可主动进入） |

网段约定序列：**2.x=打印、3.x=裁判**；新增路由器先脱机改段再入网（默认网段常撞 31.x → rogue DHCP 全网断网）。

## 三、4A 打印机隔离区（OpenWrt 23.05.2）

- 详细访问方式与踩坑：`qianli-lab-network` skill §九。状态：已改造完成，**wan 待插主网**（插上即 DHCP）。
- 防火墙三规则（按 31.x 现实编址）：
  1. `Allow-Mgmt-MainLAN`：192.168.31.0/24 → 本机 22/80/443（管理面）；
  2. 主网→打印区：31.0/24 → 192.168.2.0/24 全放行（小电脑管打印机）；
  3. 打印区→小电脑：192.168.2.x → 仅 192.168.31.57（打印机主动回推）。
  打印区→外网/其余主网主机：全拒绝。
- WiFi：`Printer`（2.4G，WPA2，ap_isolate=1 客户端隔离）；`Printer-5G` 禁用。
- 待办：wan 插线验证 → 打印机入区（屏幕开局域网模式，抄 IP/Access Code/SN/机型）→ 4A 配静态租约 → `bambu-print-reservation` 的 `PRINTER_HOSTS` 填 2.x 地址 → 端到端打印分发实测。

## 四、裁判系统路由器

- 状态：已入网（wan=192.168.31.80）。WiFi `裁判系统` 已启用。品牌型号待补，管理入口 192.168.3.1。
- 拓扑探测：运维台 `NET_TARGETS` 已收录（ICMP 探活；wan 侧 TCP 管理口被其防火墙挡属预期，勿用 TCP 探测）。
- 通信矩阵：
  | 源 → 目标 | 状态 | 依赖 |
  |---|---|---|
  | 物理机器人/裁判端(3.x) → 小电脑(31.57) | ✅ 全通 | NAT 出站 + 小电脑防火墙规则 `Referee-Zone-192.168.3.0-24`（已加） |
  | 小电脑(31.57) → 物理机器人(3.x) | 🟡 待端口转发 | 裁判系统官方软件发布后，在其后台按端口加转发（主网关无回程路由） |
  | 3.x → 外网 | ✅ 可通（双重 NAT） | 机甲大师裁判端为纯内网应用，一般用不到 |
- 待办：①其 wan 改静态 IP（192.168.31.80 / 掩码 255.255.255.0 / 网关+DNS 192.168.31.1）固化地址；②软件发布后按端口清单加转发。

## 五、小电脑上的跨区防火墙（Windows）

| 规则名 | 内容 | 用途 |
|---|---|---|
| `Referee-Zone-192.168.3.0-24` | 入站放行来自 192.168.3.0/24 的所有协议 | 裁判区物理机器人/裁判端访问小电脑后端 |

（打印区 2.x 的回推目前走小电脑主动发起的 MQTT 长连接，无需独立入站规则；若未来打印机要主动连新服务，仿照上条加 `192.168.2.0/24` 规则。）

## 六、运维台账

- 拓扑探测：运维台「🌐 网络拓扑」/ `GET :3100/api/network`；主网设备全量清单=主网关 miwifi API `misystem/devicelist`（比 `/api/network/lan` 代理稳定）。
- 管理入口：主网关 `192.168.31.1`（miwifi）；4A=有线插其 LAN 口 + IPv6 链路本地（skill §九）；裁判路由器=`192.168.3.1`（3.x 内）。
- 备份：`Desktop\qianli-backups\4a-openwrt-backup-20260920.tar.gz`（4A 改造前全量配置，工作区外不入 git）。
- 待办汇总：①4A wan 插线 + 运维台 `NET_TARGETS` 补 4A 条目（wan 入网配静态地址后，管理口经 `Allow-Mgmt-MainLAN` 可探测；裁判路由器条目已加，ICMP 探活）；②打印机入区五连（见 §三）；③裁判系统软件发布后端口转发；④笔记本拓展坞静态 IP `192.168.31.2` 建议改 `192.168.3.2`（需管理员，便于以后直连裁判路由器）。
