# 机器人总成使用指南（功能逻辑持续维护手册）

**「机器人总成」= 本地运维台**（`dashboard/`，http://127.0.0.1:3100）：把 qianli 项目群六个机器人装到一块面板上——看状态、查日志、改配置、发部署，都在这台面板上点选完成。本指南是它的使用说明，同时也是**各机器人功能逻辑的持续维护手册**：每个机器人做什么、逻辑在哪个文件/哪份文档、改逻辑与改配置分别走什么流程、有哪些不能碰的红线。

> 配套文档：各机器人职能边界看顶层 `AGENTS.md`；权能/指令/端口**单一事实来源**看 `dashboard/registry.js`；动态广场看板搭建看《动态广场看板搭建指南.md》。
>
> **分工与同步义务**：面向全体成员的版本是桌面《机器人总成使用指南.html》（`C:\Users\0d00\Desktop\`，工作区外、不进 git）——凡功能、指令、播报时刻、卡片样式、联动、看板/面板发生**用户可感知的改动**，必须同批同步进那份 HTML（保持其面向成员的口吻，维护者流程不搬进去）。本手册面向维护者；两份文档的事实口径都以 registry.js 与各仓 README/LOGIC-MAP 为准。

---

## 一、启动与访问

```bash
cd dashboard
npm install   # 首次
npm start     # → http://127.0.0.1:3100
```

- **仅本机**：只绑定 127.0.0.1，不部署 NAS、不对外暴露；
- **NAS 凭据直读 `approval-bot/.env`**（`NAS_HOST/PORT/USER/PASSWORD`），不复制、不入库——运维台上所有 NAS 数据（pm2 状态、日志、定制窗口）都靠它走 SSH；
- 页面打开期间每 4 秒刷新总览，状态采样约 25~30 秒一针，采样史落盘 `.status-history.json`（保留 3 天，重启不丢）。

## 二、能看什么（界面导览）

### 1. 总览仪表台（页面顶部）

| 区块 | 看什么 | 口径 |
| --- | --- | --- |
| KPI 指标条 | NAS 进程、HTTP 健康、本地测试进程、24h 平均可用率、近24h活跃、未提交改动、动作执行 | 一眼判断全局健康 |
| 📡 服务状态矩阵 | 每服务：当前状态与持续时长、HTTP 深度健康点、内存条、运行时长/重启数、24h 可用率、近 1h 时间线 | 状态四色：**绿=NAS 在线，橙=仅本地测试进程，红=离线，灰=未知**（NAS 不可达，不计入可用率）；点服务名跳详情卡片 |
| 👥 队员活跃 | 近 24h/7天 活跃人数与消息量、Top 成员、功能使用分布 | 数据源网关使用统计 `GET /api/usage`（按天保留 30 天），谁 open_id→通讯录姓名、什么功能 |
| 🧩 功能激活 | 各域 policy 窗口聚合：网关长连接状态、hub 两张回答表启停与规则数、duty 管辖群/名册/白名单人数、approval 审批流程与三条催办计划 | bambu/ticket 的 policy 窗口未上线前显示占位 |
| ⚠️ 近 24h 掉线事件 | 连续「离线」时段聚合成事件（起止 + 时长），仍在持续的标红 | 「NAS 与本地皆无进程」才算掉线 |

**两个容易误读的口径**：
- **HTTP 深度健康点**专抓「pm2 显示 online 但接口已僵死」——pm2 绿点 + HTTP 红点 = 该重启了；
- **可用率/掉线事件只在运维台页面打开时采样**——页面没开的时段没有数据，不是服务没掉线。

### 2. 项目卡片区（页面下方，每机器人一张卡）

每张卡固定展示：**端口与名称、职能一句话、部署方式、监听链路、指令/接口清单、权限边界、📌 备注、git 分支与最新提交、未提交改动数**，以及一排操作按钮。卡片内容全部来自 `registry.js`——**在这里看到的职能描述就是权威口径**。

### 3. 日志抽屉（点按钮弹出）

三种日志：**本地日志**（本地测试进程实时输出，内存环形缓冲 600 行，不落盘）、**动作输出**（npm push / stub 测试等一次性命令的完整输出）、**NAS pm2 日志**（SSH tail，out 120 行 + error 40 行，可调）。

## 三、能做什么（操作）

### 1. 本地测试进程启停

点卡片上「本地测试启动」在本机跑起该机器人，自动注入 `QUIET_HOURS_DISABLED=1`（手动测试不受晚间静默限制）。本地实例**收不到生产消息事件**（事件在 NAS 内网由网关闭环转发），用于调 `/api/bot/test-*` 干跑接口和看启动日志。

> ⚠️ **feishu-gateway 没有本地启动按钮，这是刻意的**：本地起网关会持有一条独立长连接，与 NAS 抢唯一长连接，把生产事件随机分流走。

### 2. 快捷动作（卡片按钮，带确认）

| 项目 | 动作 |
| --- | --- |
| 全部 | npm install |
| hub | 值日分支 stub 测试 |
| duty-bot | 排班+闭环+策略 stub 测试（四连）、表格字段校验、动态广场建表（幂等，可重复执行） |

### 3. npm push 部署（核心发布入口）

点「npm push 部署」→ 输入**提交说明** → 二次确认 → 执行 `npm run push`。链路 = **git 提交 → GitHub → SFTP 传 NAS → pm2 重启**（qianli-deploy 统一链路；bambu 无 git 步骤，纯 SFTP）。执行中按钮变「动作输出中…」，结束后看「上次动作输出」确认成功，再回状态矩阵确认该服务变绿、HTTP 点变绿。

- 提交说明要写清改动内容——它同时是 git commit message 和 DEVLOG 版本说明的素材；
- push 卡在 git push 网络错误（连接超时/SSL 中断）→ 走 git-push-accelerator 加速后重试；其它部署失败放顶层会话排查。

### 4. NAS 重启

卡片上「NAS 重启」= `pm2 restart <名> --update-env && pm2 save`（带确认）。用于接口僵死（pm2 online 但 HTTP 红点）等现场救火；改了 `.env` 的重启不解决配置下发——env 改动必须走 push。

### 5. 🧰 定制中心（卡片右上「🧰 定制」按钮展开）

读写经 `/api/nas/api`（SSH curl）直达 NAS 本机接口，**增删即时生效、不重启**：

| 机器人 | 窗口 | 能做什么 |
| --- | --- | --- |
| duty-bot | 白名单 | 值日排除名单加人/移人（即时生效）；「原始 JSON」看全文 |
| duty-bot | 名册 | 查看全景；「立即执行」触发通讯录同步（约 6 秒，open_id 直取组织架构） |
| duty-bot | 管辖策略 | 管辖群 chat_id 逗号分隔在线改写（即时生效，清空=不限制）；显示当前是「env 默认」还是「在线改写生效中」 |
| duty-bot | 值日简报 / policy / roster | 只读查看 |
| hub | 关键词回答表 | **可视化编辑器**：群消息表（未@生效）/ @触发表两表切换、增改规则（同关键词组覆盖）、删除、整表启停；改动写 NAS `.local.json` 即时生效 |
| approval-bot | policy | 只读全景：审批流程、审批人白名单、三条催办计划（含 cron 时刻与宽限天数） |
| ticket / bambu | — | policy 窗口**未落地**（已登记顶层待办，别写成已有） |

**⚠️ hub 回答表的覆盖规则**：界面编辑落在 NAS 的 `.local.json`（即时生效），但 **npm push 会用本地 xlsx 版覆盖它**。临时增删直接在界面点；**批量/持久编辑以本地 `关键词回答表.xlsx` 为源**，改完随 push 上线。

## 四、六大机器人功能逻辑速查

### 端口与部署总表

| 端口 | 项目 | pm2 进程 | 代码位置 | 部署方式 |
| --- | --- | --- | --- | --- |
| 3010 | feishu-gateway（事件接入层） | feishu-gateway | `feishu-gateway/` | SFTP 直传（顶层仓归档） |
| 3000 | project-management-robot（对话枢纽 hub） | knowledge-tracker | `ticket-pm/project-management-robot/server/` | npm run push |
| 3001 | bambu-print-reservation（打印预约） | bambu-print-server | `bambu-print-reservation/` | 纯 SFTP，无 git 步骤 |
| 3002 | approval-bot（财务审批） | approval-bot | `approval-bot/` | npm run push |
| 3003 | ticket-bot（工单域） | ticket-bot | `ticket-pm/ticket-bot/` | npm run push |
| 3006 | duty-bot（值日域） | duty-bot | `duty-bot/` | npm run push |
| 3100 | dashboard（本运维台） | —（本机 node） | `dashboard/` | 不部署，仅本机 |

### feishu-gateway · 事件接入层（:3010）

- **职能**：全工作区唯一飞书长连接，统一收事件并路由分发；其它服务一律 `FEISHU_USE_LONG_CONNECTION=false`；
- **路由规则**（按序匹配，命中即停）：消息默认 → hub；例外①工单域（`/ticket-*` 指令或 @机器人+「接单」）→ ticket-bot；②审批事件 approval_instance/task → bambu（打印分发）+ ticket-bot（接单→审批自动通过）；③多维表格变更 → 按 `CONSUMERS` 登记广播；
- **附带能力**：使用统计（`GET /api/usage?days=N`，按天 30 天，落盘项目外 `/home/qianli/feishu-gateway-data/`），每 30 分钟自动 upsert 到机器人项目看板三张网关表（`POST /api/usage-sync/run?force=1` 手动补数）；
- **改逻辑看**：`feishu-gateway/README.md`「消息事件路由 / 审批事件 / 新增一个飞书机器人项目的步骤」；改路由/消费者登记**先读** `.agents/skills/qianli-chat-architecture/SKILL.md`；
- **排障归属**：「指令时灵时不灵 / @没反应 / 事件被抢」→ 先查网关（连接状态、路由表、消费者登记）。

### project-management-robot（hub）· 对话枢纽 + 项目管理（:3000）

- **职能**：所有对话/指令的唯一入口——@对话、基础指令、关键词自动回答、DDL 播报与逾期确认、会议提醒、项目表、每日语录；
- **消息管道分支顺序**（改任何对话逻辑前必须知道）：
  - 私聊：DDL 逾期确认回复 → 值日分支 → 基础指令（白名单）→ 对话；
  - 群聊：@对话 → 值日群分支（看板带不带 / 均可、@+纯图片静默、非管辖群值日指令回提示）→ 审批群分支（仅放行 /approval-*）→ 基础指令 → 关键词自动回答；会议卡片提醒对值日管辖群跳过（群级功能全关，严格按 duty-bot 下发管辖群命中判定）；
- **定时任务**：DDL 每日播报（默认 12:00，4 群分组；「未结单工单」分栏数据调 ticket-bot 的 `unclosed-by-group` API）、逾期私聊确认、会议卡片提醒；
- **指令转发**：`/approval-*` → approval-bot:3002，`/print-*` → bambu:3001，值日口语指令 → duty-bot:3006（都走 `POST /api/chat/command`）；
- **定制窗口**：`GET /api/hub/policy` 全景 + 回答表两表 CRUD（见定制中心）；关键词监听群配置 `/api/keywords/config`；
- **改逻辑看**：`ticket-pm/LOGIC-MAP.md` §2 + 本项目 README「功能特性」。

### ticket-bot · 工单域（:3003）

- **职能**：播报到组别群、@接单确认、搬运看板、结单提醒、未结单分桶 API；**架构铁律唯一例外**（允许直接消费工单域消息事件）；
- **播报主链路**：监听多维表格「审批节点」事件 → 按「是否指定人员负责」分支播报；项目搬运有 category 门控；
- **接单闭环**：@机器人+「接单/接单N」事件驱动确认（同群多张排队区分序号）→ 确认后审批任务机器代理自动通过（审批人解析失败不缓存、自愈重试）；
- **定时任务**（每小时整点重扫，Asia/Shanghai）：6 小时未接单超时处理、理想结单时间过后结单提醒、指定负责人 24h 未确认私聊追问。**晚间静默内整轮跳过，09:00 整点轮次天然就是冲刷**（重扫类以补发时刻最新数据重查，夜里已了结的事不再播）；
- **对外 API**：未结单按负责人组别分桶 `unclosed-by-group`（hub DDL 分栏数据源，改出入参两边同批）；
- **配置**：`GROUP_ROUTES` 群聊路由、`GROUP_LEADERS` 组长映射、审批节点监听、结单提醒参数（见 README「三、配置说明」）；
- **定制窗口**：`/api/tickets/policy` **未落地**（待办，勿写成已有）；
- **改逻辑看**：`ticket-pm/LOGIC-MAP.md` §1 + 本项目 README「一、核心业务逻辑」。

### approval-bot · 财务审批（:3002）

- **职能**：审批群 `/approval-*` 指令、每周财务催办周报（催发票/报销单/转账）、每日待审批提醒、催发票私聊；**纯定时播报，无事件即时播报**，仅服务审批群（`BOT_CHAT_ID`）；
- **三条催办链路**（时刻/宽限在 policy 窗口可见，cron 改 `.env` 后 push 生效）：周报、每日提醒、催发票私聊——私聊链路带回复轮询，识别「延期/无法提交」；
- **指令**：`/approval-help /approval-list /approval-pending /approval-status /approval-urge`（审批群 @对话型机器人，hub 转发）；
- **定制窗口**：`GET /api/approval/policy` 只读全景（流程/白名单/催办参数）；
- **改逻辑看**：README「四、播报逻辑总览（三条催办链路一览）」。

### bambu-print-reservation · 打印预约（:3001）

- **职能**：`/print-*` 指令、预约审批、打印机自动分发与控制；不消费消息事件（hub 转发 + 审批事件）；
- **主链路**：官方审批表单提交 → 审批通过（或机器人按 AMS 匹配自动同意）→ 网关秒级推 approval_instance/task → 解析表单入队（加急优先）→ 打印机空闲触发 AMS 装料匹配（材料精确→家族、颜色 redmean 近似、指定打印机优先）→ 下载 3mf → SFTP/FTP 上传 + MQTT 下发 → gcodeState 变迁监听开始/完成/失败 → 群 webhook 播报；分发失败按重试上限退队转人工；
- **注意**：多维表格「打印预约表」只是半小时级镜像，主通道下**改表格状态不会触发分发**；
- **指令**：`/print-help /print-status /print-ams /print-list /print-pending /print-dispatch <编号> <打印机名>`；
- **部署特殊性**：push 无 git 步骤，版本锚点取顶层仓库中该路径的归档提交；
- **定制窗口**：`/api/print/policy` **未落地**（待办）；
- **改逻辑看**：README「工作流 / 分发匹配规则 / 事件链路」。

### duty-bot · 值日域（:3006）

- **职能**：排班生成与轮转/缺勤补偿、私信提醒与收口、照片凭证写表、值日助手（私信说明/群看板）、管辖策略下发、名册自动读通讯录；
- **定时任务**（Asia/Shanghai，全部过静默闸门）：**D-1 20:00 预告、当日 12:00 今日值日看板自动播报（webhook 优先，失败回退应用身份直发管辖群）、18:30 提醒、22:00 收口（写表不延迟）、00:30 对账**；
- **指令**（hub 转发，带 / 前缀等效）：`值日助手`（私聊用法说明/群看板，1h 限流）、`我要请假`、`查询我的下一次值日`、`绑定 姓名`、`是 / 否`（口语变体「是的/好了/完成了/做完了/搞定」在有当日询问会话时等同「是」）、`生成排班表`（名册 admin 专用）；
- **权限与数据**：名册自动同步通讯录全员（open_id 直取），`whitelist.json` 为值日排除名单；**真实名册/白名单权威在 NAS 侧**，不进 git，push.js 上传前有备份+守卫（本地条目少于现网自动跳过并告警）——见顶层 AGENTS「运行时数据保护」；
- **定制窗口**：`GET|POST /api/duty/policy`（管辖群在线改写）、roster 全景/refresh、whitelist 增删、`GET /api/duty/brief` 昨日/今日简报；
- **待办澄清**：`/api/duty/brief` 数据接口已上线但暂无消费方（「昨日值日播报」= M4 未实施，hub `.env` 的 `DUTY_WEBHOOK_URL` 等为预留键）；
- **改逻辑看**：README「排班规则 / 定时任务 / 值日助手 / API」。

## 五、功能逻辑持续维护工作流

### A. 改功能逻辑（发版类，走 push）

1. **对号入座**：按顶层 AGENTS「项目职能划分总表」确认需求归属，发错立即停；
2. **读现状**：该机器人 README / `ticket-pm/LOGIC-MAP.md` 对应章节；涉消息路由/指令转发/@识别，**先读** qianli-chat-architecture skill；
3. **改代码并履行同步义务**（见 C）；
4. **本地验证**：运维台「本地测试启动」+ stub 测试按钮 + `/api/bot/test-*` 干跑（本地不受静默限制）；
5. **运维台「npm push 部署」**：写清提交说明 → 二次确认；
6. **发后验证**：「上次动作输出」无报错 → 状态矩阵变绿、HTTP 深度健康点变绿 → 需要的话点「NAS 日志」确认启动日志；
7. **记 DEVLOG**：本项目 `DEVLOG.md` 文末追加 `## vN · 日期 · 提交哈希 · 类型`（vN 只增不复用；多项目联动各记一条 + 顶层联动摘要）。

### B. 改定制配置（不发版，走定制中心）

- 能热改的：duty 白名单/管辖群、hub 回答表（规则增删改、整表启停）——**即时生效**；
- 只读/需发版的：approval policy、各域 env 配置——改 `.env` 后走 push；
- 两个易踩点：hub 回答表 push 会被本地 xlsx 覆盖（批量编辑以 xlsx 为源）；duty 管辖群在线改写后显示「override 生效中」，要回到 env 默认就清空重存前先核对 `.env` 值。

### C. 文档同步义务（改了什么就要同步什么）

| 改动 | 必须同步 |
| --- | --- |
| 权能/指令/监听/端口/定制窗口 | `dashboard/registry.js`（**单一事实来源**，运维台卡片与定制中心直接读它） |
| 需求归属/易混裁定变化 | 顶层 `AGENTS.md` 职能总表 |
| 播报时机/新增播报点 | 对应 LOGIC-MAP/README 写清静默窗口行为 |
| 定制窗口增删 | `registry.js` 的 `windows` + 顶层 AGENTS「定制窗口」现状行 |

### D. 红线（改逻辑前过一遍）

1. **架构铁律**：除工单接单监听，所有对话逻辑由对话型机器人触发；专项机器人不得自行消费消息事件，网关不得给它们加消息直连路由；
2. **晚间静默**：02:00–09:00 定时/自动播报一律积压到 09:00 补发——新增定时播报接 `gateTask`，事件通知接 `gatePayload`；可重扫任务冲刷时以最新数据重跑；挤压要为挤压之后的事情负责；
3. **运行时数据保护**：清空/删除/覆盖 NAS 侧用户数据（名册、白名单、回答表 `.local.json`、多维表格数据）之前**先备份并征询用户**，禁止静默覆盖；
4. **建表/改表**：一律改 `duty-bot/scripts/create-plaza-tables.js`（幂等），禁止手改线上表结构造成与脚本不同步。

## 六、日常巡检节奏

**每天打开运维台扫一眼（1 分钟）**：
1. KPI：NAS 进程 `x/x`、HTTP 健康 `x/x`（有红点=僵死，重启对应进程）、24h 平均可用率；
2. ⚠️ 近 24h 掉线事件：有标红未结束的事件先看 NAS 日志定位；
3. 未提交改动：黄色数字提醒有代码没 push——要么发版要么提交，别让改动过夜堆积；
4. 改动过配置的日子：回看「功能激活」面板核对（回答表启停/规则数、管辖群数量、催办计划）。

**每周**：
1. 👥 队员活跃与功能使用分布——哪个功能没人用、哪张回答表该补充；
2. 各卡片 git 分支/最新提交与 DEVLOG 对得上，无「改了没记版」；
3. 「🧰 定制中心」过一遍各域 policy 全景，核对白名单/名册/管辖群与实际人员变动一致（名册点一次通讯录刷新）。

## 七、常见问题（FAQ）

**Q1：某机器人没响应，第一眼看哪里？**
状态矩阵该行：pm2 状态列 + HTTP 深度健康点。都绿 → 看是「事件没到」（先查网关长连接与路由）还是「业务分支没命中」（回 LOGIC-MAP 对应管道顺序）。pm2 online 但 HTTP 红点 → 接口僵死，「NAS 重启」。

**Q2：为什么 gateway 不能本地测试启动？**
本地实例会持有第二条长连接与 NAS 抢事件，生产消息被随机分流。网关问题用 NAS 日志 + `/api/health`、`/api/usage` 观察。

**Q3：关键词回答在界面上改好了，push 一次后被旧内容覆盖？**
push 用本地 `关键词回答表.xlsx` 覆盖 NAS `.local.json`。界面适合即时小改；要持久的改动，同步回本地 xlsx 再 push。

**Q4：可用率 100% 但我记得昨晚服务挂过？**
采样只在运维台页面打开时进行。页面没开的时段没有采样数据，掉线事件/可用率不统计该时段。

**Q5：状态灰色「未知」是什么？**
NAS 不可达（SSH 失败）期间无法判断，不计入可用率分母。先看页头「NAS 在线/不可达」。

**Q6：本地测试进程会影响生产吗？**
不会。本地实例收不到生产事件（事件在 NAS 内网闭环），且自动禁用晚间静默便于测试；唯一例外 gateway（见 Q2）。

**Q7：想加一个新机器人？**
按 feishu-gateway README「新增一个飞书机器人项目的步骤」接入；不自行连飞书；建目录后在 `registry.js` 登记（端口/职能/指令/窗口），运维台自动出卡。
