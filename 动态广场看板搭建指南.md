# 动态广场看板 · 搭建指南

> 位置：飞书「机器人项目看板」多维表格（app_token `ZlVZbXDkRayUzSsFRiycznmZn5b`）→ 顶部「仪表盘」→「新建仪表盘」。
> 数据已自动流入，仪表盘图表因开放平台限制无法用 API 创建，按下表逐个点选即可（约 10 分钟）。
> 参考文档（wiki）暂无法读取（共享应用缺 `wiki:node:read` 权限），本清单按标准多维表格仪表盘模式设计；开通权限后可对照调整。

## 已就绪的数据源

**既有表（直接引用，无需迁移）**

| 表 | 数据 | 适用图表 |
| --- | --- | --- |
| 【27赛季】千里工单系统 | 41 条工单：申请状态/审批流程/面向组别/发起与完成时间 | 状态分布、组别分布、处理时长 |
| tbl_project | 116 个项目：status/priority/category/ddl | 状态分布、DDL 临期 |
| tblxuanyun_project | 20 个宣运项目 | 状态分布 |
| 值日看板 | 值日记录：完成状态/该组总状态 | 完成率 |
| tbl_keyword | 203 条关键词监听发言 | 发言活跃（可选） |

**新建表（数据自动流入）**

| 表 | table_id | 写入方 | 频率 |
| --- | --- | --- | --- |
| 动态广场 | `tbld1zHXkTzko20p` | ticket-bot/bambu/duty-bot/hub 事件钩子 | 事件实时 |
| 网关日活跃 | `tbl6BeFsM1WcOUjl` | gateway 每 30 分钟 upsert（签名去重；**日期为日期类型**，支持动态筛选；**口径=机器人交互**：显式路由命中/私聊/@机器人，群内未 @ 闲聊不计） | 近实时 |

> 「网关功能使用」「网关队员活跃」两表已于 2026-09-13 下线（监听口径收敛为机器人交互，分功能/分队员明细不再落表）：多维表格里的两张表与仪表盘上基于它们的图表（功能使用 TopN、队员活跃 TopN）已随之移除，勿再重建。

## 仪表盘图表清单（逐个添加）

**指标卡（图表类型：指标卡）**

| # | 指标 | 数据表 | 配置 |
| --- | --- | --- | --- |
| 1 | 工单总数 | 【27赛季】千里工单系统 | 统计字段=申请编号，计数 |
| 2 | 进行中工单 | 同上 | 同上 + 过滤：审批流程 ≠ 已完成/已拒绝（按实际单选项选） |
| 3 | 进行中项目 | tbl_project | 统计字段=name，计数 + 过滤：status = 进行中（按实际单选项） |
| 4 | 今日机器人交互 | 网关日活跃 | 统计字段=总消息数（即当日机器人交互次数），求和 + 过滤：日期 = **今天**（日期为日期类型，直接选动态「今天」即可） |

**饼图/环图（分布）**

| # | 图表 | 数据表 | 分组 |
| --- | --- | --- | --- |
| 5 | 工单申请状态分布 | 工单系统 | 分组=申请状态，计数 |
| 6 | 项目状态分布 | tbl_project | 分组=status，计数 |
| 7 | 值日完成情况 | 值日看板 | 分组=完成状态，计数 |

**柱状/条形（对比）**

| # | 图表 | 数据表 | 配置 |
| --- | --- | --- | --- |
| 8 | 工单按面向组别 | 工单系统 | 分组=面向组别（多选会拆分），计数，降序 |
| 9 | 项目按类别 | tbl_project | 分组=category，计数 |

**折线（趋势）**

| # | 图表 | 数据表 | 配置 |
| --- | --- | --- | --- |
| 10 | 网关日活跃趋势 | 网关日活跃 | X=日期，Y=总消息数（折线）；可加第二条 Y=活跃人数 |

**动态广场 feed**

- 不用图表：左侧数据表「动态广场」即事件流，建议把视图按「动态时间」倒序排序（表格视图设置里改一次即可）；
- 仪表盘内也可加一个「指标卡：今日动态数」（动态广场，计数 + 过滤动态时间=今天）。

## 维护说明

- 网关日活跃表由 gateway 自动 upsert（口径=机器人交互；手动补数：NAS 上 `curl -X POST -H "X-API-Token: <GATEWAY_API_TOKEN>" 'http://localhost:3010/api/usage-sync/run?force=1'`（token 在 gateway `.env`））；签名去重，重复触发不产生重复行；
- 动态广场由各机器人事件钩子追加写入（ticket-bot 播报/接单/结单，bambu 打印四态，duty-bot 完成/请假，hub DDL 播报），失败只留日志不影响业务；approval-bot 维持现状（不再主动接入，待数据需求提出再动）
- 表结构变更请改 `duty-bot/scripts/create-plaza-tables.js`（幂等，可重复执行）。
